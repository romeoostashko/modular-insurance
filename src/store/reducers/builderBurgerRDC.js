import * as typeAction from "../actions";
const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case 1:
      return {};

    default:
      break;
  }
  return state;
};

export default reducer;
